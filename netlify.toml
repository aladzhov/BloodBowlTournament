[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist/frontend/browser"

[build.environment]
  NODE_VERSION = "22"

[dev]
  command = "cd frontend && npx ng serve --configuration=development"
  targetPort = 4200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

